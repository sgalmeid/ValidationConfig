server:
  port: ${PORT:${SERVER_PORT:0}}
spring:
  datasource:
    hikari:
      connectionTimeout: 20000
      maximumPoolSize: 5
    url: jdbc:postgresql://postgres_flight:5432/flights
    username: sga
    password: sga
  jpa:
    hibernate:
      ddl-auto: update

